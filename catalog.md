# Stapel module catalog

10 modules · 183 operations · 40 axes · 48 extension points.

Generated by `stapel-catalog` from each module's `docs/capabilities.json`. Do not edit by hand.

## Modules

### stapel-auth 0.7.4

User authentication and account access: sign-up and sign-in with email or phone one-time codes, passwords, social (OAuth) accounts, corporate SSO (SAML/OIDC), magic links, QR hand-off and passkeys; guest (anonymous) access; two-factor authentication and step-up verification; session and device management with a security audit trail.

| axis | default | ops gated |
|---|---|---|
| AUTH_ANONYMOUS | true | 1 |
| AUTH_EMAIL_LOGIN | true | 22 |
| AUTH_EMAIL_PLACEMENT | main | 0 |
| AUTH_EMAIL_REGISTRATION | true | 22 |
| AUTH_MAGIC_LINK_LOGIN | true | 2 |
| AUTH_MAGIC_LINK_PLACEMENT | overflow | 0 |
| AUTH_OAUTH_LOGIN | true | 7 |
| AUTH_OAUTH_PLACEMENT | bottom | 0 |
| AUTH_OAUTH_REGISTRATION | true | 7 |
| AUTH_PASSKEY_LOGIN | true | 6 |
| AUTH_PASSKEY_PLACEMENT | bottom | 0 |
| AUTH_PASSWORD_LOGIN | false | 10 |
| AUTH_PASSWORD_PLACEMENT | overflow | 0 |
| AUTH_PASSWORD_REGISTRATION | false | 10 |
| AUTH_PHONE_LOGIN | true | 22 |
| AUTH_PHONE_PLACEMENT | main | 0 |
| AUTH_PHONE_REGISTRATION | true | 22 |
| AUTH_QR_LOGIN | true | 5 |
| AUTH_QR_PLACEMENT | bottom | 0 |
| AUTH_SSO_LOGIN | true | 12 |
| AUTH_SSO_PLACEMENT | bottom | 0 |
| AUTH_SSO_REGISTRATION | true | 12 |
| AUTH_TOTP | true | 5 |
| OAUTH_STEP_UP | false | behavior |
| PASSWORD_LOGIN_STEP_UP | true | behavior |

**Extension points:** OAUTH_PROVIDER_CLASSES, REREGISTRATION_MODEL, AUTH_USER_MODEL, serializer_seams, STAPEL_VERIFICATION.EXTRA_FACTORS, url_factories

**Requires:** stapel-core, stapel-gdpr (optional), stapel-notifications (optional)

**Flows:** Password login (+ optional TOTP), Passwordless login (email OTP), Step-up verification on a protected endpoint (reference flow)

**React components:** @stapel/auth-react@0.4.0 — 19 hooks, 13 demos

### stapel-billing 0.4.15

Payments and billing: per-user credit wallets with an immutable transaction ledger, one-off credit packages and recurring subscription plans, Stripe-backed checkout and self-service customer portal, idempotent webhooks and a service-to-service debit endpoint.

| axis | default | ops gated |
|---|---|---|
| PAYMENT_PROVIDER | stapel_billing.providers.stripe.StripeProvider | 0 |

**Extension points:** CREDIT_PACKAGES, PLANS, PAYMENT_PROVIDER, AUTH_USER_MODEL, serializer_seams

**Requires:** stapel-core, stapel-auth (optional)

**Flows:** — (not documented)

**React components:** @stapel/billing-react@0.4.3 — 8 hooks, 4 demos

### stapel-calendar 0.3.8

Calendar and scheduling: events with participants and RSVP, RFC 5545 recurring series (virtual expansion, on-demand materialization, per-instant cancellation and reschedule), availability windows with free/busy and bookable slot computation, ICS export, and event-driven reminders.

| axis | default | ops gated |
|---|---|---|
| VISIBILITY | participants | behavior |

**Extension points:** REMINDER_POLICY, SCOPE_PROVIDER, PRESETS, calendar.occurrence.materialized, serializer_seams

**Requires:** stapel-core, stapel-auth (optional), stapel-notifications (optional)

**Flows:** — (not documented)

**React components:** @stapel/calendar-react@0.4.1 — 9 hooks, 4 demos

### stapel-chat 0.1.6

Conversations and messaging: direct (1:1, idempotent by participant pair), group and support threads on one model; text/system messages with a monotonic per-conversation seq, replies and opaque CDN attachment keys; per-participant read markers and unread counts; anchor-paginated history (canonical anchor=seq) and conversation lists; a support layer (unassigned queue, first-come assignment, open/pending/resolved with reopen); and an optional Channels consumer for realtime delivery with seq-replay resume.

| axis | default | ops gated |
|---|---|---|
| ATTACHMENTS | true | behavior |
| CHAT_KINDS | [direct, group, support] | behavior |
| MAX_BODY_LENGTH | 4000 | behavior |

**Extension points:** SCOPE_PROVIDER, chat.message, chat.support.assigned, serializer_seams

**Requires:** stapel-core, stapel-auth (optional), stapel-cdn (optional)

**Flows:** — (not documented)

### stapel-notifications 0.3.13

Multi-channel user notifications: routes typed notification requests to email, SMS and push with per-user preferences and language, branded email templates, device push-token management and an in-app notification feed; delivery is logged per channel with idempotency.

| axis | default | ops gated |
|---|---|---|
| EMAIL_PROVIDER | mock | 0 |
| PUSH_PROVIDER | fcm | 0 |
| SMS_PROVIDER | mock | 0 |

**Extension points:** TYPES, EMAIL_TEMPLATES, channel_providers, template_overrides, serializer_seams

**Requires:** stapel-core, stapel-auth (optional), stapel-profiles (optional), stapel-translate (optional)

**Flows:** — (not documented)

**React components:** @stapel/notifications-react@0.4.0 — 3 hooks, 3 demos

### stapel-profiles 0.4.3

User profiles and social graph: display name, avatar, locale/currency/measurement preferences, notification opt-outs, and follow/unfollow/block relationships between users, with a relationship-status lookup.

| axis | default | ops gated |
|---|---|---|
| PROFILES_AVATAR_CHECK | comm | 0 |

**Extension points:** serializer_seams, profile_extras_relation, profile.changed

**Requires:** stapel-core, stapel-auth (optional), stapel-cdn (optional), stapel-gdpr (optional)

**Flows:** — (not documented)

**React components:** @stapel/profiles-react@0.4.0 — 8 hooks, 5 demos

### stapel-recordings 0.4.3

Recording lifecycle: audio/video upload (presigned single-PUT and multipart), storage, a configurable processing pipeline (convert, transcribe, diarize, merge) producing a unified speaker-attributed transcript, optional automatic summaries, and a watchdog that recovers stuck or abandoned recordings.

| axis | default | ops gated |
|---|---|---|
| SUMMARIZE_ENABLED | true | behavior |

**Extension points:** PIPELINE, STAGES, PIPELINE_RESOLVER, STORAGE, NORMALIZER, SOURCE_TYPES, serializer_seams

**Requires:** stapel-core, stapel-agent (optional), stapel-auth (optional)

**Flows:** — (not documented)

**React components:** @stapel/recordings-react@0.3.1 — 4 hooks, 4 demos

### stapel-reviews 0.1.6

Target-generic reviews and ratings: an author rates and reviews an opaque host-defined target (target_type + target_key), driven by a per-target-type policy registry (who may review, pre/post moderation, one-review-per-author, owner responses) whose authority questions are answered by host comm callbacks. The module owns the per-target aggregate (avg/count over published reviews) and emits a generic visibility-change fact carrying it, so a host catalog maintains its own rating projection without calling back.

| axis | default | ops gated |
|---|---|---|
| MODERATION_DEFAULT | post | behavior |
| RESPONSES | true | behavior |

**Extension points:** TARGET_TYPES, reviews.review.published, reviews.review.hidden, reviews.aggregate, serializer_seams

**Requires:** stapel-core, stapel-auth (optional)

**Flows:** — (not documented)

### stapel-video 0.2.3

Video calls: rooms with shareable join codes, an access-level admission model (public / scope-trusted / restricted lobby) with a realtime waiting room over WebSockets, host admit/deny controls, join-token minting through a pluggable video-provider seam (LiveKit by default), and a recording-egress seam (start/stop + a video.egress_ended event) that integrates with stapel-recordings by event, never by import.

| axis | default | ops gated |
|---|---|---|
| DEFAULT_ACCESS_LEVEL | restricted | behavior |
| DEFAULT_ADMIT_REQUIRED | true | behavior |
| VIDEO_PROVIDER | stapel_video.providers.livekit.LiveKitProvider | behavior |

**Extension points:** VIDEO_PROVIDER, SCOPE_PROVIDER, video.egress_ended, serializer_seams

**Requires:** stapel-core, stapel-auth (optional), stapel-recordings (optional)

**Flows:** — (not documented)

### stapel-workspaces 0.5.3

Team workspaces and tenancy: personal and work workspaces with owner/admin/member/viewer roles, email invitations with single-use tokens, last-owner protection, per-workspace settings and storage quotas, plus internal service-to-service membership checks — the foundational scope other modules attach their workspace_id to.

**Extension points:** Workspace.settings, AUTH_USER_MODEL, serializer_seams, role_capabilities

**Requires:** stapel-core, stapel-auth (optional), stapel-notifications (optional)

**Flows:** — (not documented)

**React components:** @stapel/workspaces-react@0.5.0 — 10 hooks, 4 demos
